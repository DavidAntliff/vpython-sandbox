#!/usr/bin/env python
from vpython import arrow, vec, color

x_arrow = arrow(pos=vec(0,0,0), axis=vec(1,0,0), color=color.red)
y_arrow = arrow(pos=vec(0,0,0), axis=vec(0,1,0), color=color.green)
z_arrow = arrow(pos=vec(0,0,0), axis=vec(0,0,1), color=color.blue)


